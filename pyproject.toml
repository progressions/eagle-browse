[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eagle-browse"
version = "0.1.5"
description = "Keyboard-first GTK browser and tools for an Eagle.cool library"
readme = "README.md"
requires-python = ">=3.11"
license = "LicenseRef-Proprietary"
authors = [{name = "Isaac Priestley"}]

[project.scripts]
eagle-browse = "app:main"
eagle-api = "cli:main"
eagle-inbox-watch = "inbox_watch:main"

[tool.setuptools]
py-modules = [
  "api",
  "app",
  "audio_crop",
  "backfill_pf_tags",
  "cli",
  "clip_editor_hand",
  "config",
  "crop",
  "filters",
  "import_media",
  "inbox_watch",
  "integrations_queue",
  "latest_job",
  "library",
  "picker",
  "pixbuf_io",
  "promptforge_stamp",
  "resources",
  "sets",
  "smart_folder_editor",
  "shutdown_gate",
  "smart_folder_rules",
  "sounds",
  "theme",
  "thumb_cache",
  "upscale_queue",
  "video_trim",
  "write",
]

[tool.setuptools.data-files]
"share/eagle-browse/sounds" = [
  "sounds/duplicate.wav",
  "sounds/error.wav",
  "sounds/notification.wav",
  "sounds/notification_play.wav",
  "sounds/remove.wav",
]
